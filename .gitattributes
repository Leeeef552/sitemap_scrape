# Track JSONL files in data/ and all its subdirectories
data/**/*.jsonl filter=lfs diff=lfs merge=lfs -text

# Track TXT files in data/ and all its subdirectories
data/**/*.txt   filter=lfs diff=lfs merge=lfs -text